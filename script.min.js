document.addEventListener('DOMContentLoaded', () => { const header = document.querySelector('.header'); window.addEventListener('scroll', () => { if (window.scrollY > 50) { header.classList.add('scrolled'); } else { header.classList.remove('scrolled'); } }); const heroBgs = document.querySelectorAll('.hero-bg'); const heroTitle = document.getElementById('heroTitle'); const heroBrandMain = document.getElementById('heroBrandMain'); const heroBrandSub = document.getElementById('heroBrandSub'); const heroDetailsTop = document.getElementById('heroDetailsTop'); const heroDetailsTitle = document.getElementById('heroDetailsTitle'); const heroDetailsStatus = document.getElementById('heroDetailsStatus'); const heroBrochureBtn = document.getElementById('heroBrochureBtn'); const heroEnquireBtn = document.getElementById('heroEnquireBtn'); const heroDots = document.querySelectorAll('.hero-dot'); function updateHeroPanel(slide) { const d = slide.dataset; if (heroTitle) heroTitle.textContent = d.title || ''; if (heroBrandMain) heroBrandMain.textContent = d.brandMain || ''; if (heroBrandSub) heroBrandSub.textContent = d.brandSub || ''; if (heroDetailsTop) heroDetailsTop.innerHTML = d.detailsTop || ''; if (heroDetailsTitle) heroDetailsTitle.textContent = d.detailsTitle || ''; if (heroDetailsStatus) heroDetailsStatus.textContent = d.detailsStatus || ''; if (heroBrochureBtn) heroBrochureBtn.href = d.scroll || '#enquire'; if (heroEnquireBtn) heroEnquireBtn.href = d.enquire || '#enquire'; } if (heroBgs.length > 0) { updateHeroPanel(heroBgs[0]); heroBgs.forEach(bg => { bg.addEventListener('click', () => { const target = bg.dataset.scroll; if (target && bg.classList.contains('active')) { const el = document.querySelector(target); if (el) el.scrollIntoView({ behavior: 'smooth' }); } }); }); } if (heroBgs.length > 1) { let currentSlide = 0; function goToSlide(idx) { heroBgs[currentSlide].classList.remove('active'); heroDots[currentSlide] && heroDots[currentSlide].classList.remove('active'); currentSlide = (idx + heroBgs.length) % heroBgs.length; heroBgs[currentSlide].classList.add('active'); heroDots[currentSlide] && heroDots[currentSlide].classList.add('active'); updateHeroPanel(heroBgs[currentSlide]); } setInterval(() => goToSlide(currentSlide + 1), 5500); heroDots.forEach(dot => { dot.addEventListener('click', () => goToSlide(parseInt(dot.dataset.index))); }); } const menuToggle = document.getElementById('menuToggle'); const mobileDrawer = document.getElementById('mobileDrawer'); const drawerOverlay = document.getElementById('drawerOverlay'); const drawerClose = document.getElementById('drawerClose'); const openDrawer = () => { mobileDrawer.classList.add('active'); drawerOverlay.classList.add('active'); document.body.style.overflow = 'hidden'; // Prevent background scrolling }; const closeDrawer = () => { mobileDrawer.classList.remove('active'); drawerOverlay.classList.remove('active'); document.body.style.overflow = ''; }; if (menuToggle) menuToggle.addEventListener('click', openDrawer); if (drawerClose) drawerClose.addEventListener('click', closeDrawer); if (drawerOverlay) drawerOverlay.addEventListener('click', closeDrawer); if (mobileDrawer) { const drawerLinks = mobileDrawer.querySelectorAll('.drawer-link, .drawer-sublink'); drawerLinks.forEach(link => { link.addEventListener('click', closeDrawer); }); } const drawerDropdownBtns = document.querySelectorAll('.drawer-dropdown-btn'); drawerDropdownBtns.forEach(btn => { btn.addEventListener('click', () => { const parent = btn.parentElement; const submenu = btn.nextElementSibling; drawerDropdownBtns.forEach(otherBtn => { if (otherBtn !== btn) { otherBtn.parentElement.classList.remove('active'); otherBtn.nextElementSibling.classList.remove('active'); } }); parent.classList.toggle('active'); submenu.classList.toggle('active'); }); }); const tabButtons = document.querySelectorAll('.tab-btn'); const projectCards = document.querySelectorAll('.project-card'); const projectsGrid = document.getElementById('projectsGrid'); const filterProjects = (filterValue) => { if (projectsGrid) projectsGrid.style.opacity = '0.3'; setTimeout(() => { projectCards.forEach(card => { const category = card.getAttribute('data-category'); if (filterValue === 'all' || category === filterValue) { card.style.display = 'block'; card.style.animation = 'fadeIn 0.5s ease forwards'; } else { card.style.display = 'none'; } }); if (projectsGrid) projectsGrid.style.opacity = '1'; }, 300); }; tabButtons.forEach(button => { button.addEventListener('click', () => { tabButtons.forEach(btn => btn.classList.remove('active')); button.classList.add('active'); const filterValue = button.getAttribute('data-filter'); filterProjects(filterValue); }); }); const dropdownItems = document.querySelectorAll('.dropdown-item, .mega-card, .drawer-sublink, .footer-links ul a'); dropdownItems.forEach(item => { item.addEventListener('click', (e) => { const category = item.getAttribute('data-filter'); if (category) { const targetTab = document.querySelector(`.tab-btn[data-filter="${category}"]`); if (targetTab) { targetTab.click(); } closeDrawer(); } }); }); const statNumbers = document.querySelectorAll('.stat-number'); const animateStats = () => { statNumbers.forEach(stat => { const target = parseInt(stat.getAttribute('data-target')); const text = stat.innerText; const suffix = text.replace(/[0-9]/g, ''); // Extract +, M+, k+ etc. let count = 0; const speed = target / 50; // speed threshold const updateCount = () => { if (count < target) { count = Math.ceil(count + speed); if (count > target) count = target; stat.innerText = count + suffix; setTimeout(updateCount, 30); } else { stat.innerText = target + suffix; } }; updateCount(); }); }; if ('IntersectionObserver' in window && statNumbers.length > 0) { const observer = new IntersectionObserver((entries, observer) => { entries.forEach(entry => { if (entry.isIntersecting) { animateStats(); observer.unobserve(entry.target); // Trigger only once } }); }, { threshold: 0.6 }); const statsRow = document.querySelector('.stats-row'); if (statsRow) observer.observe(statsRow); } else { animateStats(); } const mainEnquiryForm = document.getElementById('mainEnquiryForm'); const successMessage = document.getElementById('successMessage'); const btnSubmitEnquiry = document.getElementById('btnSubmitEnquiry'); if (mainEnquiryForm && successMessage) { mainEnquiryForm.addEventListener('submit', (e) => { e.preventDefault(); const name = document.getElementById('clientName').value.trim(); const phone = document.getElementById('clientPhone').value.trim(); const email = document.getElementById('clientEmail').value.trim(); const project = document.getElementById('preferredProject').value; if (!name || !phone || !email || !project) { alert('Please fill out all required fields.'); return; } btnSubmitEnquiry.disabled = true; const originalText = btnSubmitEnquiry.innerHTML; btnSubmitEnquiry.innerHTML = '<span>SENDING...</span>'; setTimeout(() => { mainEnquiryForm.style.display = 'none'; successMessage.style.display = 'flex'; console.log('--- ENQUIRY FORM SUBMITTED ---'); console.log(`Name: ${name}`); console.log(`Phone: ${phone}`); console.log(`Email: ${email}`); console.log(`Project: ${project}`); console.log(`Message: ${document.getElementById('clientMessage').value}`); console.log('------------------------------'); btnSubmitEnquiry.disabled = false; btnSubmitEnquiry.innerHTML = originalText; }, 1500); }); } ['about', 'magnus-about', 'project-highlights', 'project-plans', 'project-amenities', 'location-advantage', 'about-mantra'].forEach(id => { const sec = document.getElementById(id); if (!sec) return; if ('IntersectionObserver' in window) { const obs = new IntersectionObserver((entries, observer) => { entries.forEach(entry => { if (entry.isIntersecting) { sec.classList.add('visible'); observer.unobserve(entry.target); } }); }, { threshold: 0.02 }); obs.observe(sec); } else { sec.classList.add('visible'); } }); const videoSection = document.getElementById('video-showcase'); if (videoSection) { if ('IntersectionObserver' in window) { const videoObserver = new IntersectionObserver((entries, observer) => { entries.forEach(entry => { if (entry.isIntersecting) { videoSection.classList.add('visible'); observer.unobserve(entry.target); // Trigger only once } }); }, { threshold: 0.02 }); // Trigger when 2% of the section is visible videoObserver.observe(videoSection); } else { videoSection.classList.add('visible'); } } const videoTabBtns = document.querySelectorAll('.video-tab-btn'); const videoPanes = document.querySelectorAll('.video-pane'); videoTabBtns.forEach(btn => { btn.addEventListener('click', () => { videoTabBtns.forEach(b => b.classList.remove('active')); btn.classList.add('active'); const targetId = btn.getAttribute('data-target'); videoPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); } else { pane.classList.remove('active'); } }); }); }); const locationTabBtns = document.querySelectorAll('.location-tab-btn'); const locationPanes = document.querySelectorAll('.location-pane'); locationTabBtns.forEach(btn => { btn.addEventListener('click', () => { locationTabBtns.forEach(b => b.classList.remove('active')); btn.classList.add('active'); const targetId = btn.getAttribute('data-target'); locationPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); } else { pane.classList.remove('active'); } }); }); }); const locationLightbox = document.getElementById('locationLightbox'); const lightboxImg = document.getElementById('lightboxImage'); const closeLightboxBtn = document.getElementById('closeLightbox'); const zoomBtns = document.querySelectorAll('.location-visual-zoom, .location-visual-wrapper img'); if (locationLightbox && lightboxImg && closeLightboxBtn) { zoomBtns.forEach(btn => { btn.addEventListener('click', (e) => { e.stopPropagation(); let imgSrc = ''; if (btn.tagName.toLowerCase() === 'img') { imgSrc = btn.getAttribute('src'); } else { const parent = btn.closest('.location-visual-wrapper'); const img = parent.querySelector('img'); imgSrc = img.getAttribute('src'); } lightboxImg.setAttribute('src', imgSrc); locationLightbox.classList.add('active'); document.body.style.overflow = 'hidden'; }); }); const closeLightbox = () => { locationLightbox.classList.remove('active'); document.body.style.overflow = ''; setTimeout(() => { lightboxImg.setAttribute('src', ''); }, 300); }; closeLightboxBtn.addEventListener('click', closeLightbox); locationLightbox.addEventListener('click', (e) => { if (e.target === locationLightbox || e.target.classList.contains('lightbox-content')) { closeLightbox(); } }); document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && locationLightbox.classList.contains('active')) { closeLightbox(); } }); } const mapTabBtns = document.querySelectorAll('.map-tab-btn'); const mapPanes = document.querySelectorAll('.map-pane'); const mapLinkBtn = document.getElementById('btn-map-link'); mapTabBtns.forEach(btn => { btn.addEventListener('click', () => { mapTabBtns.forEach(b => b.classList.remove('active')); btn.classList.add('active'); const targetId = btn.getAttribute('data-target'); const mapLink = btn.getAttribute('data-link'); if (mapLinkBtn && mapLink) { mapLinkBtn.setAttribute('href', mapLink); } mapPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); } else { pane.classList.remove('active'); } }); }); }); const enquiryModal = document.getElementById('enquiryModal'); const btnNavEnquire = document.getElementById('btnNavEnquire'); const btnEnquiryModalClose = document.getElementById('btnEnquiryModalClose'); const enquiryForm = document.getElementById('enquiryPopupForm'); const epfSuccess = document.getElementById('epfSuccess'); function openEnquiryModal() { enquiryModal.classList.add('active'); enquiryModal.setAttribute('aria-hidden', 'false'); document.body.style.overflow = 'hidden'; } function closeEnquiryModal() { enquiryModal.classList.remove('active'); enquiryModal.setAttribute('aria-hidden', 'true'); document.body.style.overflow = ''; setTimeout(() => { if (enquiryForm) enquiryForm.reset(); if (enquiryForm) enquiryForm.style.display = ''; if (epfSuccess) epfSuccess.style.display = 'none'; }, 350); } const enquiryTriggerBtns = document.querySelectorAll('.btn-enquire, .btn-trigger-enquiry, #btnNavEnquire'); enquiryTriggerBtns.forEach(btn => { btn.addEventListener('click', (e) => { e.preventDefault(); openEnquiryModal(); }); }); if (btnEnquiryModalClose) { btnEnquiryModalClose.addEventListener('click', closeEnquiryModal); } if (enquiryModal) { enquiryModal.addEventListener('click', (e) => { if (e.target === enquiryModal) closeEnquiryModal(); }); } document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && enquiryModal && enquiryModal.classList.contains('active')) { closeEnquiryModal(); } }); if (enquiryForm) { enquiryForm.addEventListener('submit', (e) => { e.preventDefault(); const name = document.getElementById('epfName')?.value.trim(); const phone = document.getElementById('epfPhone')?.value.trim(); const email = document.getElementById('epfEmail')?.value.trim(); if (!name || !phone || !email) { [document.getElementById('epfName'), document.getElementById('epfPhone'), document.getElementById('epfEmail')].forEach(el => { if (el && !el.value.trim()) { el.style.borderColor = '#e05252'; el.addEventListener('input', () => { el.style.borderColor = ''; }, { once: true }); } }); return; } enquiryForm.style.display = 'none'; epfSuccess.style.display = 'flex'; setTimeout(closeEnquiryModal, 2500); }); } const highlightTabs = document.querySelectorAll('.highlight-tab-btn'); const highlightPanes = document.querySelectorAll('.highlight-pane'); highlightTabs.forEach(tab => { tab.addEventListener('click', () => { highlightTabs.forEach(t => t.classList.remove('active')); tab.classList.add('active'); const targetId = tab.dataset.target; highlightPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); } else { pane.classList.remove('active'); } }); }); }); const level1Tabs = document.querySelectorAll('.level1-tab-btn'); const plansPanes = document.querySelectorAll('.plans-pane'); level1Tabs.forEach(tab => { tab.addEventListener('click', () => { level1Tabs.forEach(t => t.classList.remove('active')); tab.classList.add('active'); const targetId = tab.dataset.target; plansPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); const level2Btns = pane.querySelectorAll('.level2-tab-btn'); const subPanes = pane.querySelectorAll('.sub-plans-pane'); level2Btns.forEach((btn, idx) => { if (idx === 0) { btn.classList.add('active'); } else { btn.classList.remove('active'); } }); subPanes.forEach((sp, idx) => { if (idx === 0) { sp.classList.add('active'); } else { sp.classList.remove('active'); } }); } else { pane.classList.remove('active'); } }); }); }); plansPanes.forEach(pane => { const level2Btns = pane.querySelectorAll('.level2-tab-btn'); const subPanes = pane.querySelectorAll('.sub-plans-pane'); level2Btns.forEach(btn => { btn.addEventListener('click', () => { level2Btns.forEach(b => b.classList.remove('active')); btn.classList.add('active'); const targetSubId = btn.dataset.target; subPanes.forEach(sp => { if (sp.id === targetSubId) { sp.classList.add('active'); } else { sp.classList.remove('active'); } }); }); }); }); const melangeElevSlides = document.querySelectorAll('#melangeElevationSlides .plan-slide'); const melangeElevDots = document.querySelectorAll('#melangeElevDots .plan-slide-dot'); const btnMelangeElevPrev = document.getElementById('btnMelangeElevPrev'); const btnMelangeElevNext = document.getElementById('btnMelangeElevNext'); if (melangeElevSlides.length > 1 && btnMelangeElevPrev && btnMelangeElevNext) { let currentElevIndex = 0; function goToElevSlide(idx) { melangeElevSlides[currentElevIndex].classList.remove('active'); melangeElevDots[currentElevIndex] && melangeElevDots[currentElevIndex].classList.remove('active'); currentElevIndex = (idx + melangeElevSlides.length) % melangeElevSlides.length; melangeElevSlides[currentElevIndex].classList.add('active'); melangeElevDots[currentElevIndex] && melangeElevDots[currentElevIndex].classList.add('active'); } btnMelangeElevPrev.addEventListener('click', (e) => { e.stopPropagation(); goToElevSlide(currentElevIndex - 1); }); btnMelangeElevNext.addEventListener('click', (e) => { e.stopPropagation(); goToElevSlide(currentElevIndex + 1); }); melangeElevDots.forEach(dot => { dot.addEventListener('click', (e) => { e.stopPropagation(); goToElevSlide(parseInt(dot.dataset.index)); }); }); } const amenitiesTabBtns = document.querySelectorAll('.amenities-tab-btn'); const amenitiesPanes = document.querySelectorAll('.amenities-pane'); amenitiesTabBtns.forEach(btn => { btn.addEventListener('click', () => { amenitiesTabBtns.forEach(b => b.classList.remove('active')); btn.classList.add('active'); const targetId = btn.getAttribute('data-target'); amenitiesPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); } else { pane.classList.remove('active'); } }); }); }); const setupAmenitiesSlider = (paneId, prevBtnId, nextBtnId) => { const pane = document.getElementById(paneId); const prevBtn = document.getElementById(prevBtnId); const nextBtn = document.getElementById(nextBtnId); if (pane && prevBtn && nextBtn) { const sliderOuter = pane.querySelector('.amenities-slider-outer'); if (sliderOuter) { prevBtn.addEventListener('click', () => { sliderOuter.scrollBy({ left: -260, behavior: 'smooth' }); }); nextBtn.addEventListener('click', () => { sliderOuter.scrollBy({ left: 260, behavior: 'smooth' }); }); } } }; setupAmenitiesSlider('amenities-melange', 'btn-melange-amenities-prev', 'btn-melange-amenities-next'); setupAmenitiesSlider('amenities-magnus', 'btn-magnus-amenities-prev', 'btn-magnus-amenities-next'); const prelaunchTabBtns = document.querySelectorAll('.prelaunch-tab-btn'); const prelaunchPanes = document.querySelectorAll('.prelaunch-pane'); prelaunchTabBtns.forEach(btn => { btn.addEventListener('click', () => { prelaunchTabBtns.forEach(b => b.classList.remove('active')); btn.classList.add('active'); const targetId = btn.getAttribute('data-target'); prelaunchPanes.forEach(pane => { if (pane.id === targetId) { pane.classList.add('active'); } else { pane.classList.remove('active'); } }); }); }); });